# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['backend_service.py'],
    pathex=[],
    binaries=[],
    datas=[('runs\\tt100k_yolov8s_stage2_1280\\weights\\best.pt', 'model')],
    hiddenimports=['backend.label_map_zh'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='backend_service',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='backend_service',
)
